# CAS WebSocket Dispatcher

This project is the backend server that monitors the `cas` database and dispatches events via WebSockets to connected desktop clients.

## Requirements
See `requirements.txt`
